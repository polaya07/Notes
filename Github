#Create a new branch 
git checkout -b [namebranch]

#Push the branch on github
git push origin [namebranch]

#Check branches
git branch -a

#Delete branches
git branch -d [namebranch]

#Undo commitments not pushed
git reset HEAD~ #If you add --soft flag before HEAD~, you will undo just the last commit
